<!-- recieve -->

<% if user_signed_in? %>
<%= form_with(scope: :search, url: search_path, method: :get, local: true) do |f| %>

      <%= f.text_field :searched_item %>

      <%= f.select :target, [["User", "1"],["Book", "2"]], prompt: "Select" %>

      <%= f.submit 'Search' %>

<% end %>
<% end %>