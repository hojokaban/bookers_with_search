<!-- recieves user -->

<%= form_for(current_user.active_relationships.new) do |f| %>
	<%= hidden_field_tag :followed_id, user.id %>
	<%= f.submit "Follow", class:"btn btn-success col-xs-12" %>
<% end %>